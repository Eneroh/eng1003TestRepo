// Learned about static int and isPrime() functionality, nothing of serious note
// code that returns static int value via return
// look up static more

int main()  {
    static int count = 10;
    return count++;
}

// static means that the item only gets incremented once or only activates once, or doesnt change outside of the scope {}
// no matter what
